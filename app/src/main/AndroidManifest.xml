<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gestion_Etablissment"
        tools:targetApi="31">

        <!-- Declare UpdateStudentActivity here -->
        <activity
            android:name=".UpdateStudentActivity"
            android:exported="false" />  <!-- Make sure the 'exported' attribute is set to true/false based on your needs -->

        <activity
            android:name=".UpdateProfessorActivity"
            android:exported="false" />
        <activity
            android:name=".UpdateStaffActivity"
            android:exported="false" />
        <activity
            android:name=".AddModuleActivity"
            android:exported="false" />
        <activity
            android:name=".AddStaffActivity"
            android:exported="false" />
        <activity
            android:name=".AddProfessorActivity"
            android:exported="false" />
        <activity
            android:name=".AddStudentActivity"
            android:exported="false" />
        <activity android:name=".UpdateModuleActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
